/**
 * T-123 — `context.js` için kısmi bildirim (yalnız TS testlerinin
 * kullandığı yüzey). Diğer dışa aktarımlar (collectContext, deviceClass…)
 * TS'ten kullanılacaksa önce buraya bildirim ekle.
 */
export function routeTemplate(path: string): string;
